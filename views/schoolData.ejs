<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All the schools</title>
</head>
<body>
    <h1>Schools</h1>
    <div class="container">

        <% for (var i = 0; i < schools.length; i ++) { %>
            <div class="card">
                <p class="data"><span class="sub-heading">School Name:</span> <%=schools[i].schoolName  %></p>
                <p class="data"><span class="sub-heading">School Address:</span> <%=schools[i].schoolAddress  %></p>
                <p class="data"><span class="sub-heading">School Email:</span> <%=schools[i].schoolEmail  %></p>
                <form action="/admin/schoolData/<%=schools[i].schoolName %>" method="post">
                    <button type="submit">Check-in</button>
                </form>
            </div>
            <% } %>
    </div>

    <style>
        .sub-heading {
            font-weight: 600;
        }
        h1 {
            margin-top: 3vw;
            font-size: 3.5vw;
            color: #16e16e;
            text-align: center;
        }
        .container {
            width: 100%;
            height: auto;
            display: flex;
            gap: 3.5vw;
            flex-wrap: wrap;
            align-items: center;
            justify-content: center;
            margin-top: 7vw;
            margin-bottom: 7vw;
        }
        .card {
            /* padding: 0.5vw; */
            padding: 1vw 0;
            background: #111;
            border-radius: 15px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 30%;
            transition: all ease-in-out 0.5s;
        }
        .card:hover {
            color: #000;
            background: #16e16e;
            animation: shake 0.3s ease-in-out infinite;
            scale: 110%;
        }
        @keyframes shake {
            0% {
                transform: rotateZ(0.5deg);
            }
            25% {
                transform: rotateZ(-0.5deg);
            }
            50% {
                transform: rotateZ(0.5deg);
            }
            75% {
                transform: rotateZ(-0.5deg);
            }
            100% {
                transform: rotateZ(0.5deg);
            }
        }
        .data {
            display: block;
        }
    </style>
</body>
</html>